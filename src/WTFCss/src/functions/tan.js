/**
 * Calculates the tangent of a given angle in degrees.
 * @param {number|string} value - The angle in degrees for which to calculate the tangent.
 * @returns {string} - A CSS value string representing the tangent of the angle.
 */
export const tan = (value) => `tan(${value})`;
export default tan;