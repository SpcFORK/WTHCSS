/**
 * Calculates the cosine of the given angle.
 * @param {number|string} angle - The angle in degrees or radians.
 * @returns {string} A CSS function representing the cosine of the angle.
 */
export const cos = angle => `cos(${angle})`
export default cos