/**
 * Calculates the logarithm of a value.
 * @param {number} value - The value to calculate the logarithm for.
 * @returns {string} The constructed CSS log() function string.
 */
export const log = value => `log(${value})`
export default log