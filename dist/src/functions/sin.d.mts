/**
 * Calculates the sine of a given angle in degrees.
 * @param {number|string} value - The angle in degrees for which to calculate the sine.
 * @returns {string} - A CSS value string representing the sine of the angle.
 */
const sin = (value) => `sin(${value})`;

export { sin as default, sin };
