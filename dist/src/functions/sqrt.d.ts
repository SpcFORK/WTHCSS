/**
 * Calculates the square root of a given value.
 * @param {number|string} value - The value for which to calculate the square root.
 * @returns {string} - A CSS value string representing the square root of the value.
 */
const sqrt = (value) => `sqrt(${value})`;

export { sqrt as default, sqrt };
